# ── Dependencies ──────────────────────────────────────────
node_modules/

# ── Environment / Secrets ─────────────────────────────────
# .env holds real secrets (DB passwords, API keys, etc.)
# NEVER commit this file. Use .env.example as the template.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
.env.test

# ── Logs ──────────────────────────────────────────────────
logs/
*.log
npm-debug.log*

# ── SQLite database file (runtime-generated, not committed) ──
data/hospital.db
data/hospital.db-shm
data/hospital.db-wal

# ── OS & Editor junk ──────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
